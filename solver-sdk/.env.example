# Velox Solver SDK Environment Variables

# =============================================================================
# Required Configuration
# =============================================================================
# Movement network RPC URL (used as fallback if Shinami not configured)
MOVEMENT_RPC_URL=https://testnet.movementnetwork.xyz/v1

# Velox contract address
VELOX_ADDRESS=0x951cb360d9b1d4cb4834cf76e4fca0f63a85237874d8b2d45b3056439b91cbb7

# Solver private key (Ed25519 hex format, without 0x prefix)
SOLVER_PRIVATE_KEY=your_solver_private_key_here

# =============================================================================
# Shinami Integration (Recommended for production)
# =============================================================================
# Get your API key from https://app.shinami.com
# Used for Node Service - provides enhanced RPC reliability for solver operations
SHINAMI_KEY=your_shinami_api_key

# =============================================================================
# Optional Configuration
# =============================================================================
# GraphQL endpoint for faster intent discovery (if available)
# GRAPHQL_URL=https://your-indexer.com/graphql

# Intent polling interval in milliseconds (default: 10000)
# POLLING_INTERVAL=10000

# Skip existing intents on startup (default: true)
# SKIP_EXISTING_ON_STARTUP=true
